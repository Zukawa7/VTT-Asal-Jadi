# Contributing

1. Create a branch for the change.
2. Install dependencies with `npm install --include=dev`.
3. Run `npm run type-check`, `npm run lint`, `npm run build`, and `npm test`.
4. Keep secrets and `data/` out of commits.
5. Preserve existing Socket.IO event names and public API compatibility.
6. Use focused commits with descriptive messages.
